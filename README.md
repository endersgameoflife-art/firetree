# firetree
# a new type of code. I hope you like this guys and girls.
# so beginners guide-type(Hello world!)= Hello world!

# var1=Tom is var2=16 type(var1) type(var2).=Tom is 16.

# type(math(8+4)) =12

# type(What's your name?) textbox[placeholder-Type name][modeifClick-useristype else mode-rest{whenEnterclicked-whatwastyped=name then type(Hello,(name)!)] =Whats you name?  "Ender" Hello,Ender! note-you cna type someething.

# if cursor in range(5) from pos(0,0) then type(You are close.)= described-When your cursor goes near a range of 5 from 0,0, the program says "You are close." note- the description is not there in the program.

# loop(type(Hello, world!)every(0.5sec)) result-Hello world! every 0.5 seconds=Hello, world! Hello, world! Hello, world!...

# func(file(type-word)(name(u7uje799MUH)openfile) result-whatever the text document had in it. 
# ok so basically type help() for all commands.
# also this does a lot of stuff. see you next time.
